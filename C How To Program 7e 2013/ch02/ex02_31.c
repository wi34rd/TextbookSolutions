#include <stdio.h>

int main(void) {
    int number, square, cube;
    
    printf("number\tsquare\tcube\n");
    
    number = 0;
    square = number * number;
    cube = square * number;
    printf("%d\t%d\t%d\n", number, square, cube);

    number = 1;
    square = number * number;
    cube = square * number;
    printf("%d\t%d\t%d\n", number, square, cube);

    number = 2;
    square = number * number;
    cube = square * number;
    printf("%d\t%d\t%d\n", number, square, cube);

    number = 3;
    square = number * number;
    cube = square * number;
    printf("%d\t%d\t%d\n", number, square, cube);

    number = 4;
    square = number * number;
    cube = square * number;
    printf("%d\t%d\t%d\n", number, square, cube);

    number = 5;
    square = number * number;
    cube = square * number;
    printf("%d\t%d\t%d\n", number, square, cube);

    number = 6;
    square = number * number;
    cube = square * number;
    printf("%d\t%d\t%d\n", number, square, cube);

    number = 7;
    square = number * number;
    cube = square * number;
    printf("%d\t%d\t%d\n", number, square, cube);

    number = 8;
    square = number * number;
    cube = square * number;
    printf("%d\t%d\t%d\n", number, square, cube);

    number = 9;
    square = number * number;
    cube = square * number;
    printf("%d\t%d\t%d\n", number, square, cube);

    number = 10;
    square = number * number;
    cube = square * number;
    printf("%d\t%d\t%d\n", number, square, cube);


    return 0;
}